import { Route, BrowserRouter as Router, Routes } from 'react-router-dom';

function AppRouter() {
  return (
    <Router>
      <Routes>
        <Route path="/"></Route>
      </Routes>
    </Router>
  );
}

export default AppRouter;
